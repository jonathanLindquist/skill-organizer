{
  "name": "agent-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-sync": "bin/agent-sync.js",
    "skill-organizer": "bin/skill-organizer.js"
  },
  "scripts": {
    "lint": "node --check bin/agent-sync.js && node --check bin/skill-organizer.js && node --check src/cli.js && node --check src/providers.js && node --check src/sync.js && node --check test/sync.test.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
